import Hero from "../components/Hero/Hero";
import { Layout } from "../layout/Layout";

const Home = () => {
  return (
    <Layout>
      <section>
        <Hero />
      </section>
    </Layout>
  );
};

export default Home;
